import { defineConfig } from 'astro/config';

export default defineConfig({
    site: 'https://marck028.github.io/Pagina-Javi/',
    base: '/Pagina-Javi/',
    trailingSlash: 'always', // <- importante para GitHub Pages
});

